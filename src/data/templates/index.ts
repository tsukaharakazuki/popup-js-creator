import type { TemplateDefinition } from '../../types/template';

import promoBanner from './promo-banner';
import productCarousel from './product-carousel';
import largeImageBanner from './large-image-banner';
import newsletterSignup from './newsletter-signup';
import announcementBar from './announcement-bar';
import couponDiscount from './coupon-discount';
import cookieConsent from './cookie-consent';
import surveyNps from './survey-nps';
import exitIntentOffer from './exit-intent-offer';
import slideInWidget from './slide-in-widget';

export const templates: TemplateDefinition[] = [
  promoBanner,
  productCarousel,
  largeImageBanner,
  newsletterSignup,
  announcementBar,
  couponDiscount,
  cookieConsent,
  surveyNps,
  exitIntentOffer,
  slideInWidget,
];
